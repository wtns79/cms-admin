{
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.4.2",
    "@fortawesome/free-solid-svg-icons": "^6.4.2",
    "@fortawesome/react-fontawesome": "^0.2.0",
    "antd": "^5.12.1",
    "axios": "^1.6.2",
    "http-proxy-middleware": "^2.0.6",
    "immer": "^10.0.3",
    "jwt-decode": "^4.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.20.0",
    "react-scripts": "^5.0.0",
    "use-immer": "^0.9.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "react-scripts": "latest"
  },
  "main": "index.js",
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build:old": "react-scripts --openssl-legacy-provider build",
    "build": "cross-env NODE_ENV=production react-scripts --openssl-legacy-provider build ",
    "build:prod": "cross-env NODE_ENV=production react-scripts --openssl-legacy-provider build "
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
